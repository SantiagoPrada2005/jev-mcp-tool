{
  "name": "jev-mcp-tool",
  "version": "1.0.0",
  "description": "Stateful remote MCP server on Cloudflare Workers backed by typesafe/jev System 1 evaluator",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "mcp",
    "ai",
    "system1",
    "typesafe-jev"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260922.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.136.3"
  }
}
